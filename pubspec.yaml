name: matrusparsh_app
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  another_flutter_splash_screen: ^1.2.0
  firebase_database: ^10.3.4
  firebase_auth: ^4.13.0
  google_sign_in: ^6.1.6
  intro_slider: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
